using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DegreeMark : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
       //movimentar os botoes, cada botao clicado ira fornecer pontos e se auto destroir(?)
    }
}
